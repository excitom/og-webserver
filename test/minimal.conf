# This is the bare minimum config file, which will serve up the 
# default page to any server name on port 8080.
#
# The server_tokens directive is actually optional, but a safe security option.
#
events {
}
http {
	server {
		server_tokens off;
	}
}

